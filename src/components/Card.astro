---
import type { CollectionEntry } from "astro:content";
import { slugifyStr } from "@/utils/slugify";
import { getPath } from "@/utils/getPath";
import Datetime from "./Datetime.astro";

type Props = {
  variant?: "h2" | "h3";
} & CollectionEntry<"blog">;

const { variant: Heading = "h2", id, data, filePath } = Astro.props;

const { title, description, ...props } = data;
---

<li class="border-b border-border/70 py-6 last:border-b-0">
  <Datetime class="mb-2 text-foreground/55" {...props} />
  <a
    href={getPath(id, filePath)}
    class:list={[
      "inline-block text-xl leading-snug font-semibold text-foreground transition-colors hover:text-accent",
      "decoration-dashed underline-offset-4 hover:underline",
      "focus-visible:no-underline focus-visible:underline-offset-0",
    ]}
  >
    <Heading
      style={{ viewTransitionName: slugifyStr(title.replaceAll(".", "-")) }}
    >
      {title}
    </Heading>
  </a>
  <p class="mt-2 text-sm leading-7 text-foreground/75 sm:text-base">
    {description}
  </p>
</li>
